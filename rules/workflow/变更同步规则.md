# 变更同步规则

跨模块改动必须做纵向同步。

- 后端接口变更：同步路由、CRUD、模型、前端 API、页面调用、测试和接口文档。
- 前端调用变更：同步后端响应契约和错误处理。
- 数据库变更：同步模型、迁移、初始化、CRUD 和测试。
- 配置变更：同步 `.env.example`、配置读取逻辑和文档。
- UI 基础组件变更：同步所有调用方和视觉回归风险点。

不要把接口代码、前端调用和文档拆成彼此不完整的提交节点。
