# 测试规则

- 新增功能或修复 bug 时同步补充测试。
- 不要为了通过测试而放宽断言；优先修复业务逻辑。
- 集成测试依赖外部 API 时必须有跳过保护。
- 已知兼容性问题使用 `xfail` 标注原因，不直接删除测试。
- 公共 fixture 优先放在 `conftest.py`。
- 测试数据放在 `tests/fixtures/`。
- 前端组件测试优先使用 jsdom 和 `@vue/test-utils`。
